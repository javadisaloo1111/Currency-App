<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- HTTPS is mandatory: cleartext traffic is blocked for every endpoint. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
